# KPI 逻辑优化说明

## 1. KPI 计算逻辑优化
- **最低分限制**：每项 KPI 的得分最低为 **0 分**，不再出现负分。
- **最高分限制**：得分最高不超过该项指标的**权重值**。
- **计算公式**：`Score = Math.max(0, Math.min((Actual / Target) * Weight, Weight))`。

## 2. 排名算法优化
- **多级排序**：在单项指标排名中，如果两名员工的**分数相同**，系统将自动根据**完成度**（实际值 / 目标值）进行二次排序。
- **优势**：确保了排名的公平性，鼓励在相同得分下完成度更高的表现。

## 3. 数值精度控制
- **四位小数**：系统中所有的计算结果（单项得分、总分、完成度）以及导入导出的数值，均统一保留**最多四位小数**。
- **应用范围**：
  - 页面实时计算显示。
  - Excel 导出数据。
  - Excel 导入数据解析。

## 4. 动态权重导入导出
- **严格遵循数据**：
  - **导出**：导出文件中的“权重%”列将严格反映当前面板中为每位员工设置的实际权重。
  - **导入**：导入时，系统会优先读取 Excel 表格中的“权重%”列。如果表格中提供了权重，系统将使用该权重覆盖默认设置；如果表格中未提供，则沿用系统默认权重。
- **灵活性**：支持不同员工针对同一指标拥有不同的权重设置。

## 5. 总结
通过本次优化，KPI 仪表板在数据的**准确性**、**公平性**以及**灵活性**上得到了显著提升，能够更好地适应复杂的考核场景。
