# KPI仪表板 - 使用指南

## 项目修改概述

本项目已完成以下功能修改：

1. **导出功能改为Excel格式** - 导出包含完整KPI数据的Excel文件
2. **导入功能改为完全替换** - 导入Excel时会完全替换现有员工数据
3. **取消导出模板功能** - 简化功能，只保留实际数据导出
4. **本地数据保存** - 数据自动保存到本地文件系统

## 快速开始

### 安装依赖
```bash
cd kpi_dashboard
pnpm install
```

### 开发模式运行
```bash
npm run dev
```
访问 `http://localhost:5173` 查看应用

### 生产模式构建和运行
```bash
npm run build
npm start
```
应用将在 `http://localhost:3000` 运行

## 功能使用说明

### 导出Excel数据

**步骤**：
1. 在"数据输入"标签页中填充员工KPI数据
2. 点击"导出Excel"按钮
3. 系统会自动下载 `KPI数据_YYYY-MM-DD.xlsx` 文件

**导出文件包含**：
- 员工名称
- 每个指标的实际值、目标值、权重%、得分
- 员工总分

**文件格式示例**：
```
员工名称 | 财富管理_目标达成率(实际) | 财富管理_目标达成率(目标) | 财富管理_目标达成率(权重%) | 财富管理_目标达成率(得分) | ... | 总分
---------|------------------------|------------------------|------------------------|------------------------|-----|------
员工1   | 95                    | 100                  | 15                   | 14.25                | ... | 85
```

### 导入Excel数据

**重要提示**：导入会**完全替换**现有员工数据，请先导出备份！

**步骤**：
1. 准备Excel文件，确保格式正确
2. 在"数据输入"标签页中点击"导入数据"按钮
3. 选择要导入的Excel文件
4. 系统会完全替换现有数据并显示导入的员工数量

**Excel文件要求**：
- 第一列必须是"员工名称"
- 必须包含指标列，格式为：`${category}_${indicator.name}(实际)`
- 可选列：`(目标)` 和 `(权重%)`
- 数值列应为数字格式

**支持的文件格式**：
- .xlsx (推荐)
- .xls

### 保存数据

**步骤**：
1. 修改员工数据后点击"保存数据"按钮
2. 系统会将数据保存到本地文件
3. 显示"✅ 数据已保存到文件！"提示

**自动保存**：
- 系统会在每次修改数据时自动保存到本地文件
- 刷新页面时会自动加载已保存的数据

## 数据存储

### 本地文件存储
- **存储位置**：`/data/employees.json`
- **格式**：JSON格式
- **自动保存**：数据修改时自动保存
- **自动加载**：页面加载时自动恢复

### API端点
- `POST /api/employees/save` - 保存员工数据
- `GET /api/employees/load` - 加载员工数据
- `POST /api/templates/save` - 保存模板数据
- `GET /api/templates/load` - 加载模板数据

## 测试

### 使用测试样本
项目包含一个测试样本文件：`test_import_sample.xlsx`

**测试步骤**：
1. 启动应用
2. 点击"导入数据"按钮
3. 选择 `test_import_sample.xlsx`
4. 验证3名员工的数据是否正确导入
5. 点击"导出Excel"验证导出功能

### 验证完整流程
1. **导出测试**：
   - 填充一些数据
   - 导出为Excel
   - 验证Excel内容正确

2. **导入测试**：
   - 导出现有数据
   - 修改Excel中的数据
   - 导入修改后的文件
   - 验证数据是否完全替换

3. **保存测试**：
   - 修改数据
   - 点击"保存数据"
   - 刷新页面
   - 验证数据是否被加载

## 常见问题

### Q: 导入时提示"Excel文件格式不正确"
**A**: 检查以下几点：
- 第一列是否为"员工名称"
- 指标列名称格式是否正确
- 文件是否为有效的Excel格式

### Q: 导入后数据丢失了
**A**: 这是正常的，因为导入会完全替换现有数据。建议：
- 导入前先导出备份
- 确认导入的文件内容正确

### Q: 刷新页面后数据消失了
**A**: 检查以下几点：
- 是否点击了"保存数据"按钮
- 服务器是否正常运行
- 检查浏览器控制台是否有错误信息

### Q: 导出的Excel中没有数据
**A**: 检查以下几点：
- 是否填充了员工数据
- 是否设置了指标值
- 检查浏览器控制台是否有错误信息

## 技术细节

### 前端技术栈
- React 19 + TypeScript
- Vite 7 (构建工具)
- XLSX 库 (Excel处理)
- TailwindCSS (样式)
- Recharts (图表)

### 后端技术栈
- Express.js (Web框架)
- Node.js (运行时)
- 文件系统 (数据存储)

### 数据流
```
用户输入 → React组件 → 自动保存 → Express API → 文件系统
                ↓
            导出Excel
            导入Excel
            加载数据
```

## 修改历史

### 2026-01-14
- 修改导出功能为Excel格式
- 修改导入功能为完全替换现有数据
- 取消导出模板功能
- 修复TypeScript编译错误
- 提交到GitHub

## 支持

如有问题或建议，请提交Issue或Pull Request到GitHub项目。

## 许可证

MIT License
