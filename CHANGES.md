# KPI仪表板 - 数据导入导出功能修改总结

## 修改日期
2026年1月14日

## 修改内容

### 1. 取消导出模板功能
- **文件**: `client/src/pages/Home.tsx`
- **修改**: 删除了 `exportTemplate()` 函数
- **UI变化**: 移除了"导出模板"按钮
- **原因**: 用户要求简化功能，只保留实际数据导出

### 2. 修改导出功能 - 改为Excel格式
- **文件**: `client/src/pages/Home.tsx`
- **函数**: `saveData()` 
- **修改内容**:
  - 将导出格式从CSV改为Excel(.xlsx)
  - 导出内容包括: 员工名称、各指标(实际值、目标值、权重%、得分)、总分
  - 表头格式: `${category}_${indicator.name}(实际)` 等
  - 每个指标包含4列: 实际值、目标值、权重%、得分
  - 最后一列为总分

### 3. 修改导入功能 - 完全替换现有数据
- **文件**: `client/src/pages/Home.tsx`
- **函数**: `handleImportFile()`
- **修改内容**:
  - 导入逻辑改为完全替换现有员工数据
  - 从Excel中读取所有员工信息
  - 如果Excel中的员工名称在系统中不存在，则创建新员工
  - 如果员工存在，则重置其数据后重新填充
  - 支持导入的列: 实际值、目标值、权重%
  - 导入成功后显示导入的员工数量

### 4. 数据保存到本地
- **文件**: `server/index.ts`
- **功能**: 已有的文件保存接口继续使用
- **API端点**:
  - `POST /api/employees/save` - 保存员工数据到JSON文件
  - `GET /api/employees/load` - 从JSON文件加载员工数据
- **存储位置**: `/data/employees.json`

### 5. 修复的Bug
- **文件**: `server/index.ts`
- **修复**: 添加了缺失的 `createServer(app)` 调用
- **文件**: `client/src/pages/Home.tsx`
- **修复**: 添加了缺失的 `Save` 图标导入

## 功能流程

### 导出Excel数据
1. 用户点击"导出Excel"按钮
2. 系统生成包含所有员工KPI数据的Excel文件
3. 文件名格式: `KPI数据_YYYY-MM-DD.xlsx`
4. 包含的列:
   - 员工名称
   - 每个指标的: 实际值、目标值、权重%、得分
   - 总分

### 导入Excel数据
1. 用户点击"导入数据"按钮
2. 选择Excel文件(.xlsx或.xls)
3. 系统读取文件中的所有员工数据
4. **完全替换**现有员工列表
5. 导入成功后显示导入的员工数量
6. 自动保存到本地文件

### 保存数据
1. 用户点击"保存数据"按钮
2. 系统将当前员工数据保存到本地文件
3. 显示保存成功提示

## 导入导出文件格式

### Excel文件结构
```
| 员工名称 | 财富管理_指标1(实际) | 财富管理_指标1(目标) | 财富管理_指标1(权重%) | 财富管理_指标1(得分) | ... | 总分 |
|---------|-------------------|-------------------|-------------------|-------------------|-----|------|
| 员工1   | 100               | 100               | 10                | 10                | ... | 85   |
| 员工2   | 95                | 100               | 10                | 9.5               | ... | 80   |
```

## 技术细节

### 前端修改
- 使用 `xlsx` 库处理Excel文件
- 导出使用 `XLSX.utils.aoa_to_sheet()` 和 `XLSX.writeFile()`
- 导入使用 `XLSX.read()` 和 `XLSX.utils.sheet_to_json()`

### 后端保存
- 使用Express.js处理API请求
- 数据保存为JSON格式
- 存储在 `/data/employees.json` 文件中

## 测试建议

1. **导出测试**:
   - 填充员工数据后导出Excel
   - 验证Excel中的数据正确性
   - 检查所有指标和总分是否正确计算

2. **导入测试**:
   - 导出现有数据
   - 修改Excel中的数据
   - 导入修改后的Excel
   - 验证数据是否完全替换

3. **数据保存测试**:
   - 修改数据后点击"保存数据"
   - 刷新页面验证数据是否被加载
   - 检查 `/data/employees.json` 文件内容

## 向后兼容性

- 新的导入格式与旧的导出格式兼容
- 旧的导出模板格式不再支持（已删除导出模板功能）
- 建议用户使用新的导出格式进行数据交换

## 注意事项

1. 导入时会**完全替换**现有数据，请谨慎操作
2. 建议在导入前先导出备份
3. Excel文件必须包含"员工名称"列作为第一列
4. 数值列应为数字格式，不要使用文本格式
